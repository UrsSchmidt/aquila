#!/usr/local/bin/aq

Write If false: 'NOT OK' Else: 'OK' EndIf;

Write If true: 'OK' Else: 'NOT OK' EndIf;

Write If true: 'OK'
      ElseIf true: 'NOT OK'
      Else: 'NOT OK'
      EndIf;

Write If false: 'NOT OK'
      ElseIf true: 'OK'
      Else: 'NOT OK'
      EndIf;

Write If false: 'NOT OK'
      ElseIf false: 'NOT OK'
      Else: 'OK'
      EndIf;
