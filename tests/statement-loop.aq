#!/usr/local/bin/aq

n := 3;
Loop:
    Write IntToStr(n);
    n := n - 1;
While n > 0:
    Write IntToStr(n);
EndLoop

n := 3;
Loop:
    Write IntToStr(n);
    n := n - 1;
While n > 0 EndLoop

n := 3;
While n > 0:
    Write IntToStr(n);
    n := n - 1;
EndWhile
