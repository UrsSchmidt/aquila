#!/usr/local/bin/aq

n := 3;
Loop:
    Write int2str(n);
    n := n - 1;
While n > 0:
    Write int2str(n);
EndLoop

n := 3;
Loop:
    Write int2str(n);
    n := n - 1;
While n > 0 EndLoop

n := 3;
While n > 0:
    Write int2str(n);
    n := n - 1;
EndWhile
