#!/usr/local/bin/aq

for i from 2 to 100 (
    numbers[i] := true;
    for j from 2 to sqrt(i) (
        if i mod j = 0 (
            numbers[i] := false;
            # TODO break;
        )
    )
)

write dict2str(numbers);
