#!/usr/local/bin/aq

m := 0;
n := 1;
write int2str(n);
for i from 1 to 10 (
    # TODO support parallel assignment?
    # m, n := n, m + n;
    s := m + n;
    m := n;
    n := s;
    write int2str(n);
)
